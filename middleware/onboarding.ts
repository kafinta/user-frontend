// NOTE: This middleware is not currently used.
// Onboarding logic is handled by the auth middleware and individual page components.
// Kept for reference if needed in the future.

export default defineNuxtRouteMiddleware(() => {
  // Placeholder - not in use
});